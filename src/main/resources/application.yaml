spring:
  application:
    name: ms-gestion-hoja-de-vida

  profiles:
    default: dev

  mvc:
    throw-exception-if-no-handler-found: true

  web:
    resources:
      add-mappings: false

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB
